#include <stdio.h>
#include <vector>
#include <algorithm>
using namespace std;
struct nd {
    int step;
    vector<int> v;
};
int stepNum[1005];
int ans[1005];
int main() {
    int n;
    nd
    while()
    while(scanf("%d", &n) == 1 && n) {
        printf("%d\n", ans[n]);
    }
    return 0;
}
